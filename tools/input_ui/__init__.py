"""
Controller bindings for recompiled titles: the config the runtime reads, and
the UI that writes it.

    py -3 -m tools.input_ui              edit the per-user config
    py -3 -m tools.input_ui <path>       edit a config somewhere else

The runtime half is src/input/input_bindings.c; docs/technical/input-binding.md
describes the file, the UI and what is not done.
"""
